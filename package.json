{
  "name": "infinite-realms-max",
  "version": "0.4.0",
  "private": true,
  "description": "Browser-first AI interactive story platform with persistent memory, campaign creation, community games, deterministic mechanics, and multi-model AI routing.",
  "scripts": {
    "start": "node bootstrap.js",
    "dev": "node bootstrap.js",
    "test": "node tests.js",
    "check": "node --check server.js && node --check public/app.js && node tests.js"
  },
  "engines": {
    "node": ">=20"
  }
}
